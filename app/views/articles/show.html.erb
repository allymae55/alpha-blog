<div id="page-content">
    <h1 class="text-center mt-4"><%= @article.title %></h1>
    <div class="row justify-content-md-center">
        <div class="col-8 mt-4">
            <div class="card">
                <div class="card text-center shadow bg-white rounded">
                    <div class="font-italic card-header">by Alice Busch</div>
                    <div class="card-body">
                        <p class="card-text"><%= @article.description %></p>
                        <%= link_to 'Edit', edit_article_path(@article), class: "btn btn-outline-info" %> </a>
                        <%= link_to 'Delete', article_path(@article), class: "btn btn-outline-danger", method: :delete, data: {confirm:"Are you sure you want to Delete"} %> </a>
                    </div>
                    <div class="card-footer text-muted">
                        <small>Created <%= time_ago_in_words(@article.created_at) %> ago,
                            Edited <%= time_ago_in_words(@article.updated_at) %> ago </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>